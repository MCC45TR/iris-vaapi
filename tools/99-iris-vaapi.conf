# Reading legacy SM8150 coherent CAPTURE buffers from the CPU is too slow for
# the stable-surface copy required by Chrome.  Cacheable MMAP buffers include
# the vb2 DMA_FROM_DEVICE cache maintenance and sustain H.264 4K60.
options qcom_iris allow_fw_boot=1 cached_capture=1
